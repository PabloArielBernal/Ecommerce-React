import { createTheme } from "@mui/material";

export const customTheme = createTheme ({
    palette: {
        primary: {
            main: "#000000",
            secondary: "#4B4B4B"
        },
        secondary: {
            main: "#53e40f",
            secondary: "#ffa600"
        }
    }
})